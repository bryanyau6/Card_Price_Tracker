# 資料庫連線設定
# 請將此文件重命名為 .env 並填入您的 PostgreSQL 資訊

# 預設為 localhost (本機)，PostgreSQL 預設端口為 5432
DB_HOST=localhost
DB_PORT=5432

# 資料庫名稱 (請先在 pgAdmin 4 中建立此資料庫，例如命名為 tcge_db)
DB_NAME=tcge_db

# 您的 PostgreSQL 使用者名稱 (預設通常是 postgres)
DB_USER=postgres

# 您的 PostgreSQL 密碼 (安裝時設定的密碼)
DB_PASSWORD=your_password_here
